# secrets
.env
.env.*
!.env.example

# WhatsApp session (contains your account's long-lived keys — NEVER commit)
auth/
.wwebjs_auth/
.wwebjs_cache/

# runtime state (flags, counters, QR image)
data/

# deps + logs
node_modules/
*.log
npm-debug.log*
pm2-*.log

# editor / os
.DS_Store
.vscode/
.idea/
